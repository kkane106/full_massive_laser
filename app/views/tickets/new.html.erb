
<%= form_for @ticket, :html => { :class => "sm-col-6"} do |f| %>
  <fieldset class="fieldset-reset">
    <legend class="h3 bold">Create Ticket</legend>
  <%= f.label :title %><br>
  <%= f.text_field :title, class: "block full-width field-light" %>

  <%= f.label :body %><br>
  <%= f.text_area :body, class: "block full-width field-light" %>


  </fieldset>
  <%= button_tag(type: 'submit', :class => 'button-blue') {content_tag(:strong, 'Submit')} %>
<% end %>
